import React from 'react';

const Title = () => {
    return (
        <header role="banner" aria-label="Main Title">
            <h1 tabIndex="0">Smart ToDo App</h1>
        </header>
    );
};

export default Title;